git init

git add . OR git add {filename} (means add every modification to staging area)

git commit -m “this is my first commit”

git remote add origin https://github.com/gakurujnr/Kush-Cake.git

git branch -M main

git push -u origin main
